version: '3'
services:
  cross-server-bans:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    volumes:
      - .env:/app/.env
    command: ["npm", "start"]
